{
  "cmd": "apio",
  "name": "build",
  "args": [ "build"],
  "targets": {
    "version": {
      "cmd": "apio --version"
    },
    "upload" : {
      "cmd": "apio upload"
    },
    "sim" : {
      "cmd": "apio sim"
    },
    "time": {
      "cmd": "apio time"
    }
  }
}
